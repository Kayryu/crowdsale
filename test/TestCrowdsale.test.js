const RYUToken = artifacts.require('RYUToken')
const RYUCrowdsale = artifacts.require('RYUCrowdsale')

/*
contract('RYUToken', function(accounts) {
  it('sets the first account as the contract creator', async function() {
    const contract = await RYUToken.deployed()

    const creator = await contract.owner

    assert.equal(creator, accounts[0], 'main account is the creator')
  })
})

contract('RYUCrowdsale', function(accounts) {
    it('check total raised', async function() {
      const contract = await RYUToken.deployed()
  
      const creator = await contract.fundGoal()
      const raised = 20000000000000000000
      assert.equal(creator, raised, 'check total raised')
    })
  
})
*/
